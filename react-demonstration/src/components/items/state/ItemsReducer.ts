
const initialState = {

}

const ItemsReducer = (state = initialState, action) => {

    switch (action.type) {

        default:
            return {
                ...state
            };
    }
}

export default ItemsReducer;